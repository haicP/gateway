---
title: Authorization Reference
description: Authorization is not part of the lightweight gateway runtime.
related:
  title: Related references
  description: Review the active lightweight runtime contracts.
  links:
    - gateway/reference/rest-api-reference
    - gateway/reference/config-reference
---

The lightweight forwarding and request-recording runtime does not enforce
gateway keys, RBAC, or audit-deny policy.

Clients still provide upstream provider credentials exactly as required by the
upstream provider. Those credentials are passed through to upstream and are
redacted before trace storage.
