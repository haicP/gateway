---
title: CLI JSON Contracts
description: Lightweight gateway CLI commands do not currently emit stable JSON contracts.
related:
  title: Related references
  description: Use the REST API for machine-readable trace records.
  links:
    - gateway/reference/cli-reference
    - gateway/reference/rest-api-reference
---

The lightweight gateway CLI currently exposes `serve`, `version`,
`config validate`, `shell-init`, and `wrap`.

These commands do not emit separate stable JSON schemas. For machine-readable
request records, use the REST trace APIs documented in the REST API reference.
